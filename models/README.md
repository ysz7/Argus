# Models

Place for local perception model artifacts used by `argus-perception`.

The MVP does not ship a custom Argus model; it relies on existing local OCR and
vision backends. Model weights are not committed to the repository.
