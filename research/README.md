# Research

Optional, offline research code (datasets, training, evaluation, model
export). Python and PyTorch are allowed here.

Nothing in this directory is part of the Argus runtime: Rust runs Argus;
research code may train models that Argus uses.
